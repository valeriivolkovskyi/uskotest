import { LoadEntity } from './load.entity';
import { LoadProps } from 'domain/data/load.data';

export class LogisticsLoad extends LoadEntity {
  constructor(props: LoadProps) {
    super(props);
  }
}
